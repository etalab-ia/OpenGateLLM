[pytest]
pythonpath = .
asyncio_default_fixture_loop_scope = session
markers =
    langfuse: round trips against a real Langfuse, skipped when it is not reachable
;addopts =
;    --cov=api
;    --cov-report=html
;    --cov-report=term-missing
;    --config-file=pyproject.toml
;    --cov-branch